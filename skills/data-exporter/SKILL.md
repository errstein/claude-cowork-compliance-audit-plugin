---
name: data-exporter
description: >
  Export Cowork audit logs in CSV, JSON, or Markdown format.
  Use when the user wants to export logs, download audit data,
  or share compliance data with their team or IT.
---

Read `~/.compliance-audit/audit.jsonl` and `sessions.log`, then export to the user's chosen format and location (default: Desktop).
